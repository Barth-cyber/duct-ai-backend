{
  "name": "duct-ai-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "python3 -m pip install -r requirements.txt || python -m pip install -r requirements.txt",
    "start": "python3 -m gunicorn -w 1 -b 0.0.0.0:$PORT application:app || python -m gunicorn -w 1 -b 0.0.0.0:$PORT application:app"
  }
}
