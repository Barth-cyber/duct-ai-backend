{
  "name": "duct-ai-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "python -m pip install -r requirements.txt",
    "start": "python -m gunicorn -w 1 -b 0.0.0.0:$PORT application:app"
  }
}
